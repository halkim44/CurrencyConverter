import React from "react";
import { SafeAreaView } from "react-native";
import Options from "./screens/Options";

export default () => {
  return (
    <SafeAreaView>
      <Options />
    </SafeAreaView>
  );
};
